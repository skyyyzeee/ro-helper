// Law core: search, calculator, version diff and charge formatting over server law data.
// Pure TypeScript with no UI or Tauri imports (enforced by boundary.test.ts).
// The public API grows ticket by ticket; nothing is exported yet.
export {};
